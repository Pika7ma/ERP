<Window x:Class="Lplfw.UI.TempWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lplfw.UI"
        mc:Ignorable="d"
        Title="TempWindow" Height="300" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Button Content="Export" HorizontalAlignment="Left" Margin="145,227,0,0" VerticalAlignment="Top" Width="75" Click="NewExcel"/>
        <DataGrid x:Name="dgTest" HorizontalAlignment="Left" Height="199" Margin="10,10,0,0" VerticalAlignment="Top" Width="272" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="状态" Width ="1*" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="名称" Width ="1*" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="型号" Width ="1*" Binding="{Binding Type}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
